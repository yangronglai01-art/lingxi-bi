# 数据层依赖（Python 3.11+）
duckdb>=1.0.0
pandas>=2.0.0

# 引擎层依赖
pydantic>=2.0.0          # 数据模型（QueryResult / Answer / ChartConfig）
requests>=2.28.0         # OpenAI 兼容接口 HTTP 客户端
python-dotenv>=1.0.0     # 读取 .env 环境变量

# 测试
pytest>=7.0.0

# ---------------------------------------------------------------------------
# 接口层 + 前端工作台（阶段④）
# ---------------------------------------------------------------------------
fastapi>=0.110.0           # Web API 框架
uvicorn[standard]>=0.27.0  # ASGI 服务器
streamlit>=1.32.0          # 前端工作台
plotly>=5.19.0             # 交互式图表
